# Nativo

extremely naive .NET AOT compiler. its very underdeveloped and it
produces one single C++ source file. the contents of C++ file are
pretty useless and you should not modify them. just compile it.